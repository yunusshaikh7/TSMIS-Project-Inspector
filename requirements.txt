# Runtime dependencies (pinned). Built/tested on Windows + CPython 3.11.
# The scanner itself is pure stdlib (zipfile + json); these two are the
# workbook writer and the desktop window.
openpyxl==3.1.5
# GUI shell: renders scripts/ui in an Edge WebView2 window (pulls pythonnet +
# clr_loader on Windows). The console flow (scripts/cli.py) never imports it.
pywebview==6.2.1
